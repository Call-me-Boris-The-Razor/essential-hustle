# Essential Hustle — Environment Variables
# Copy to .env.local and fill in values
#
# Required: none (site works without any env vars)
# Optional: analytics + contact webhook

# ── Umami Analytics (optional — self-hosted, privacy-friendly) ──
# NEXT_PUBLIC_UMAMI_URL=https://analytics.example.com
# NEXT_PUBLIC_UMAMI_WEBSITE_ID=xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx

# ── Contact Form Webhook (optional) ──
# Server action sends form data to this URL via POST (JSON body)
# Works with Telegram Bot API, Slack webhooks, Discord webhooks, etc.
# CONTACT_WEBHOOK_URL=https://api.telegram.org/bot<token>/sendMessage?chat_id=<id>
